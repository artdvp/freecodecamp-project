{
  "name": "personal-portfolio",
  "version": "1.0.0",
  "description": "Personal Portfolio",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html --out-dir public",
    "build": "parcel build index.html index.js index.css --out-dir dist --public-url ./"
  },
  "author": "artdvp",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^4.8.3"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.6.1",
    "parcel-bundler": "^1.7.1"
  }
}
